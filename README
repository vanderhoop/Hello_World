Hello World! Just playing around in the terminal, getting comfortable in git/GitHub! So I stage files with the <add> command, commit them with the aptly named <commit> command, and add them to the preferred remote repository using the <push> command. Lovely.